[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py setup_groups && python manage.py collectstatic --noinput && gunicorn appfundationwDj.wsgi:application --bind 0.0.0.0:8080"
